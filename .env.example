# Copy to .env (do not commit) or export in shell / GitHub Secrets.
# Domain to attach API key for (suffix match)
AYBLLC_API_DOMAIN=aybllc.org

# Secret key (keep out of logs!)
AYBLLC_API_KEY=REPLACE_WITH_REAL_KEY

# Auth style options:
#  - bearer           -> Authorization: Bearer <key>
#  - x-api-key        -> X-API-Key: <key>
#  - header:Name      -> Custom header (e.g., header:X-Auth-Token)
#  - query:param      -> Querystring (?api_key=<key>)
AYBLLC_API_AUTH_STYLE=bearer

# Optional custom names when using header:/query:
AYBLLC_API_HEADER_NAME=X-API-Key
AYBLLC_API_QUERY_NAME=api_key
